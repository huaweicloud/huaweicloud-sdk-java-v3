package com.huaweicloud.sdk.mas.v1.model;

import java.util.Objects;

/**
 * BaseMultiActiveZoneSpec
 */
public class BaseMultiActiveZoneSpec {

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        return true;
    }

    @Override
    public int hashCode() {
        return Objects.hash();
    }

    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("class BaseMultiActiveZoneSpec {\n");
        sb.append("}");
        return sb.toString();
    }

}
